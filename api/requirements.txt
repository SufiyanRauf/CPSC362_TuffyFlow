# Runtime, shipped to Vercel
fastapi
pydantic

# Local development only. Small and harmless in the bundle; the rule that matters
# is no heavy numerical libraries (numpy, pandas, scikit-learn) for arithmetic
# you can write in a loop.
uvicorn
pytest
